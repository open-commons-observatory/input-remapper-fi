# Problem registry

A problem is a recurring failure with one root cause and (usually) one fix. Many items point at one problem.

{% if problems %}
| Problem | Status | Items |
|---|---|---|
{% for p in problems %}
| [{{ p.title | cell(80) }}](problems/{{ p.id }}.md) | {{ p.status }} | {{ p.items }} |
{% endfor %}
{% else %}
No problems registered yet.
{% endif %}

## Actionable items not yet assigned to a problem ({{ unassigned | length }})

{% if unassigned %}
| Item | Potential | Title |
|---|---|---|
{% for r in unassigned %}
| [#{{ r.n }}](https://github.com/{{ vars.source }}/issues/{{ r.n }}) | {{ r.pr_potential }} | {{ r.title | cell(80) }} |
{% endfor %}
{% else %}
None.
{% endif %}
